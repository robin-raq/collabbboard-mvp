# CollabBoard — Server Environment Variables
# Copy to .env for local development

# WebSocket server port
PORT=1234

# Anthropic API key (optional — falls back to local regex parser if not set)
# Get your key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Supabase (required for persistence)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Clerk (required for multi-board auth)
CLERK_SECRET_KEY=

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173

# Langfuse Observability (optional — tracing disabled if not set)
# Sign up at https://cloud.langfuse.com to get your keys
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

